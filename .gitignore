# Dependencies
node_modules
.pnpm-store

# Next.js
.next
out
next-env.d.ts

# Production
build
dist

# Misc
.DS_Store
*.pem
.env
.env.local
.env*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode
.idea
*.swp

# Test
coverage
.vitest

# TypeScript incremental build info
*.tsbuildinfo

# NOTE: src/db/migrations/meta/ used to be ignored, which left the snapshot
# chain incoherent and broke `pnpm db:generate` after migration 0009. The
# chain is now tracked; commit any new *_snapshot.json files alongside the
# SQL migration they correspond to.

# OS
Thumbs.db

# Claude Code local config (user-specific, never commit)
.claude/
.claude-code
